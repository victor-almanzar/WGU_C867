#include <iostream>                 // this is a preprocessor directive. this indicates tha we want to use 
                                    // the contents of the standard library iostream (Input/Output Stream)
int main()                          // every program must have a 'main' fuction
{                                   // everything within curly braces is part of main function
    std::cout << "Hello World!\n";  // cout stands for character output
    return 0;                       // when program finishes running, this send '0' to OS, indicating program worked.q
}
